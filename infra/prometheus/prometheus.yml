global:
  scrape_interval: 15s
  evaluation_interval: 15s

rule_files:
  - alerts.yml

scrape_configs:
  - job_name: nodate-flow-api
    static_configs:
      - targets:
          - localhost:8080
    metrics_path: /metrics
