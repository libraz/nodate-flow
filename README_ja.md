# nodate-flow

> **タスクを「管理する」ためのツールではなく、仕事の流れが勝手に前へ進んでいくシステム。**

nodate-flow は、軽やかな Asana 代替として使い始めつつ、LLM や MCP を "あとから足した便利機能" ではなく中核の実行層として据え直している OSS のタスク基盤です。

> **名前について:** 英語の *"no date"*（締切に振り回されない働き方）と、日本語の **「野点（のだて）」**（野外で気軽に茶を点てる所作）を重ねています。厳しい期日運用ではなく、気負わず流れに身を任せて仕事が進んでいく感覚を、野点のひとときに重ねました。

**ステータス:** alpha — 開発中、正式リリース前
**ライセンス:** [AGPL-3.0](./LICENSE)
**スタック:** Go (Huma + chi + sqlc) · MySQL 9.1 · React 19 (Vite + TanStack + Tailwind v4) · 境界は OpenAPI 3.1

---

## 何を作っているのか

世の中のタスク管理ツールの多くは、タスクを一行のレコードとして扱います。`status` カラムがあって、人間がそれをクリックで動かしていく。nodate-flow はそこから離れて、タスクを **プロセス** として扱います。状態は誰かが代入するものではなく、制約とイベントから自然と **導かれる** ものです。

この発想を支える 5 つの原則があります。機能を足すかどうかの判断は、常にこの 5 つより下にあります。

1. **タスクは行ではなくプロセス。** 状態はデータとして持つのではなく、導出する。
2. **制約とイベントで動く。** `UPDATE tasks SET status = ...` のような直接的な状態書き換えはしない。
3. **LLM は飾りではなく実行ロジック。** LLM が無くても動きはするが、あるとまったく別のものになる。
4. **外部サービスは "連携先" ではなく同じ空間に置く。** GitHub Issue も Slack のスレッドも Google Doc も、すべて `signals` という同じテーブルに流れ込む。REST を叩く "integration" ではなく、MCP を通じた "internalization"。
5. **UI は操作するためではなく、眺めて必要なときだけ手を入れるためのもの。** ボタンで状態を変える画面ではなく、タイムラインと状態グラフが主役になる。

## 既存のツールと何が違うのか

| | Asana / Jira / Linear | Plane / Vikunja / OpenProject | **nodate-flow** |
|---|---|---|---|
| タスクモデル | `status` カラムを持つ行 | `status` カラムを持つ行 | 制約とイベントから導出されるプロセス |
| LLM の立ち位置 | 後付けの "AI 機能" | ほとんど無し | 中核の実行ロジック |
| 外部サービス | REST 連携 | Webhook / REST | MCP が一次の signal ソース |
| UI の思想 | 操作（クリックで状態を変える）| 操作 | 観測して、必要なときだけ介入する |
| ホスティング | プロプライエタリな SaaS | OSS、自前ホスト歓迎 | OSS (AGPL)、自前ホスト前提 |
| ライセンス | プロプライエタリ | AGPL-3.0 (Plane, Vikunja) / GPL (OpenProject) | AGPL-3.0 |

一番近いのは **Plane** です。「OSS の Asana 代替で、AGPL で、自前ホストが主役」という立ち位置はほぼ同じ。ただしタスクのモデルそのものが違っていて、Plane は依然として "行 + status" の世界観の延長にあるのに対し、nodate-flow は "制約 + イベント" を土台に置き、LLM をビルトインの実行アクターとして組み込みます。

**目指していないもの:** Asana をピクセル単位で再現すること。Cycle / Module / Goal / OKR / Portfolio / Form / Wiki / Gantt のような重厚なサブ機能群は、最初から **対象外** として置いています。そうした概念の多くは、制約とイベントをちゃんと設計すれば自然に浮かび上がってくるか、あるいは不要になる、というのがこのプロジェクトの賭けです。

## リポジトリ構成

```
apps/
  api/       # Go バックエンド (Huma + chi + sqlc)
  web/       # React 19 フロントエンド (Vite + TanStack)
  cli/       # TypeScript 製 CLI (バイナリ名: tnk)
packages/
  sdk/       # OpenAPI から生成される TypeScript SDK
  ui/        # デザインシステム
  fixtures/  # テスト用フィクスチャ
errors/      # エラーコードの一次ソース (YAML → Go + TS に生成)
sql/         # テーブル / ビュー / sqlc クエリ
```

## 起動方法

Alpha のため、安定版のリリースはまだありません。開発用の入り口だけ載せておきます。

```sh
git clone <repo>
cd nodate-flow
cp .env.example .env
docker compose up
```

## ライセンス

nodate-flow は **AGPL-3.0** で配布しています。自前ホスト、改変、再配布は自由です。ただし、改変したものをネットワーク越しに第三者へ提供する場合は、AGPL §13 によって、そのユーザーが対応するソースコードを取得できるようにする義務が生まれます。これは Plane や Vikunja、Leantime と同じ考え方です。

---

English version: [`README.md`](./README.md)
