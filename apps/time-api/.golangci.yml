run:
  timeout: 5m
  go: "1.25"

linters:
  disable-all: true
  enable:
    - gofmt
    - goimports
    - govet
    - staticcheck
    - errcheck
    - revive
    - gosec

issues:
  exclude-dirs:
    - apps/time-api/internal/db/generated
    - apps/time-api/internal/errors
