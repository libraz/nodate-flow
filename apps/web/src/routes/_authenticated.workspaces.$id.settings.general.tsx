/**
 * /workspaces/$id/settings/general — route stub. See sibling `.lazy.tsx`.
 */

import { createFileRoute } from '@tanstack/react-router';

export const Route = createFileRoute('/_authenticated/workspaces/$id/settings/general')({});
