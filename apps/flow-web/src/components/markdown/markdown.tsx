/**
 * Re-export from packages/ui. Kept for backwards-compatibility with
 * any local imports; prefer importing directly from
 * `@nodate-flow/ui/primitives/markdown`.
 */
export { default, default as Markdown } from '@nodate-flow/ui/primitives/markdown';
