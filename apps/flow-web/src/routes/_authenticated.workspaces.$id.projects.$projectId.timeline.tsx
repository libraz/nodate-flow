/**
 * /workspaces/$id/projects/$projectId/timeline — route stub.
 * See sibling `.lazy.tsx`.
 */

import { createFileRoute } from '@tanstack/react-router';

export const Route = createFileRoute('/_authenticated/workspaces/$id/projects/$projectId/timeline')(
  {},
);
